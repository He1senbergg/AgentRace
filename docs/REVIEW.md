# 正确性审查记录

round2最终独立复核：确认前向中炮空间缺陷、逐炮ID抢人；复查全局分配/后炮前墙/升级优先级。实际复现“墙位全堵但building工人持续被占用”，主Agent修复并新增释放回归。最终无新增阻塞问题。主Agent一次Windows141项PASS（15.956秒），独立审查未追加全量。真实敌人堵路、持续维修及1300生存仍待平台。

Versus独立只读复核：发现运券先于治疗、既有武器采购阻塞基地抢修两项问题，均修复并回归。最终实际diff复核无新增阻塞问题；取石目标非硬墙上限、中心朝向/L3优先属策略假设已记录。审查未运行测试。主Agent完整135项中133通过，2旧建造场景调整后对应8项专项通过；不重复全量，不宣称完整原命令PASS。game1按用户纠正红方376。

## Self5 修复最终复核

独立只读子Agent实际读取最终diff，确认已邻接炮手不能绕炮弃位、补药先于任务且健康先锋不远途采购、旧矿80%收益滞后重选、诊断仅固定商品白名单及药品数量。上述前三项来自本次独立审查发现，均已修复并新增回归。最终未发现新增阻塞问题。主Agent最终单次Windows127项PASS（15.819秒），含两个半场各1300回合观测回放；独立Agent未运行测试。实际长期生存、药品收益、真实解题成功率仍未验证。

## Self4 修复最终审查（2026-09-10）

用户授权的独立只读子 Agent 复查任务发现、截止门禁、经济和升级改动，发现两项边界错误：工作目录遍历可能先耗尽搜索额度；原始文本预算不能保证 JSON 转义后的字节上限。现已优先搜索任务根目录、按根限制目录数，并按最终 UTF-8 JSON 48KB 收束；新增大目录和控制字符回归。采购往返预算包括买用及两回合余量，复核认为保守合理。最终只读复核无新增阻塞性错误。

主 Agent 完整 Windows 120 项 PASS（15.344 秒）、WSL 120 项 PASS（22.154 秒），编译和差异空白检查通过。真实任务成功率、三火箭实战效果和既有弹道语义风险仍需平台证据。当前修改未自动提交。下文为历史审查记录。

审查方式：本会话在实现后的单独审查阶段，按规格检查代码与调用链、反例和测试证据。随后由用户授权的独立只读子Agent审查，结论见下文。

## 已核对范围

- ActionValidator的12种动作、共享预算/角色/建筑预留与拒绝原子性。
- TaskPlanner异步来源、任务描述变化、截止、命令结果与答案反馈；GameSession串行事务、回合缓存和回滚。
- NewsPlanner证据解析、配额、快照摘要；TreasurePlanner预算、容量、采购/移动窗口和失败消费。
- EconomyPlanner、DefensePlanner的调用顺序、建造/维护/控制者互斥、伤害估值与目标阵营。
- 协议字段白名单、位置/ID规范化、BFS与移动预留、商品/背包解析、严格JSON及命令结果标记。
- run.sh解释器选择、参数传递和exec。正式CentOS运行与平台行为仍无证据。

## 发现与验证

1. 已修复：targetTeam筛选错误地影响物理弹道。依据主规格§15.3和§16，前方所有存活机器人均应拦截子弹或消耗电磁能量。新增test_other_side_robot_still_intercepts_ballistic_weapons在修复前失败（错误地给后方机器人10伤害），修复后验证加特林拦截、电磁能量扣减及完整fire选点。全体机器人参与伤害估值，防守收益仍优先己方威胁。
2. 已排除：怀疑提交反馈ID类型错配。World._roles已将id规范成字符串；新增test_submission_legality_feedback_uses_json_string_actor_key直接验证true/false/缺失三种反馈，原代码无需修改。动作合法仍不等于答案正确。
3. 已记录未决：attackPower多发/溅射语义；回合末统一伤害结算对同回合后续弹道的影响；非共线擦边弹道；武器build.name和roundNo起点。均不能用本地静态模型宣称已验证。

## 最新证据

- Windows完整91项PASS，10.064秒。
- WSL CPython3.11.10完整91项PASS，17.038秒，包含实际run.sh、密集HTTP与2600回合观测回放。
- git diff --check通过。没有自动提交。

## 本地交付审计

源代码逐段复查、主程序和测试差异检查、52条需求映射及文档一致性核对已完成。独立只读审查结论在下文；正式平台联调由用户负责，仍为未验证。

文档审查已消除README的POSIX未执行误述，测试计划重整为当前91项分类与命令，保留真实历史失败和正式联调缺口；DESIGN中P3–P5待实现等过时陈述已修正。本轮只增加文档修改，没有重新运行未变更的测试代码。

交付核验：仅复制run.sh与src/main.py到临时目录，Windows与WSL CPython3.11.10真实HTTP均返回预期采矿动作，进程退出和临时目录清理成功。未改应用代码，91项全套结果仍适用；启动/人工接管说明见RUNBOOK.md。正式平台配置与联调仍缺。

已完整读取当前src/main.py相对HEAD的diff（452行新增、17行删除），逐段核对任务/新闻接入、配额门禁、伤害集合、默认成本和HTTP入口；未发现新的确定性代码错误。正式平台入口仍缺，已向用户请求测试程序路径或平台接入方式。

## 独立只读审查结论

用户授权的独立子Agent已完成当前主程序、相关AI Spec及测试审查，未修改文件，未访问Official或外网；独立执行核心六模块67项PASS（0.051秒）。未发现新的确定性协议错误或必现崩溃。默认武器名称采用roleType的变更无新增审查异议。

明确残余风险：L2加特林(10,10)，前方(11,10)机器人HP5、后方(12,10)HP40，当前两发估值会将前方预测死亡后排除，估计后方剩30；如果平台到回合末才移除前方，两发均被它拦截，后方仍40。§19统一回合末结算没有明确多发之间的存活更新，不能宣称当前物理估值已验证。此项与非共线栅格弹道、动态attackPower分开记录。

默认建造名称修改后，主Agent完整Windows92项PASS（10.308秒），WSL92项PASS（17.223秒），py_compile与git diff --check通过。原长局固定观测只有两武器，启用默认建造后持续触发第三座建设、采矿断言失败；改为三武器建成观测，保留全部采矿断言，单独新增三种默认名称验证。没有放宽动作门禁或测试断言。
