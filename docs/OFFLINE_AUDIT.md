# V2.4 离线可靠性审查（2026-09-12）

本轮从 `096c947`（sonnet5 v2.4、main、干净工作区）开始。用户授权直接审查、修复并本地验证；没有新实机材料。仅访问 `AI Spec/`、源码、测试、开发文档和 `log/Versus/`，未访问 `Official/`。默认模式仍为 defense，原 E2–E5 策略参数未重调。

## 已修复的问题

| 问题及影响 | 根因与修复 | 验证 |
|---|---|---|
| 缺钱重建时工人停工 | BUILD_WEAPON 占住工人但没有筹资动作；缺石/满包的 BUILD_WALL 也会停留在拒绝或 PAUSED。建设受阻时通过现有 ECONOMY 授权临时采矿/售矿，保留建设目标及回防门禁 | `test_audit_economy`：零现金全炮损失后采集→销售→三炮建成；失败售矿重试；满包和缺矿；晚间门禁 |
| 无人能交付的服务仍冻结资金 | 无 owner 的墙服务占住20金币，使100金币无法买100金币武器券。分配/回防决定之后，只保留有效购买 owner 的服务桶；持券、无商品、已use等待验收的目标无需第二笔购买款 | 可达/不可达墙成对测试；合法建筑环静态封锁；真实同轮买券与扣账；已持券及连续Session VERIFY回归 |
| 无法采购的角色抢占服务 owner | 没商品或满包且没有可售物时，原分配仍可选该角色。现在分配前排除；有券角色仍可配送 | 满包先锋重新分配给工人、缺商品无owner；既有任务和物流专项 |
| 损炮后补错种类 | 按剩余总数索引“rocket、rocket、railgun”，丢一火箭后会再造电磁炮。统一按存活种类和已接受的当回合build计算既定组成缺口 | 存活组合、同轮双建、自定义名称及覆盖计数；无钱重建闭环 |
| 同名新任务继承旧任务状态 | defense 在任务结束后可能分配LOGISTICS/RETURN，不再调用TaskPlanner，留下旧答案、pending甚至expired。观察到无活跃任务时统一结束旧实例，归档仅为正确性未验证的经验 | 默认Session两实例同描述、已超时实例、异常回滚及重试仅归档一次；断档不捏造新实例 |
| 日志故障能导致HTTP 500 | before_request/after_request和异常处理中的日志位于原保险边界外。隔离这三处日志异常 | 前置、后置、错误日志抛OSError仍为200合法JSON；缓存和后续回合恢复 |
| 斜线射击漏算机器人拦截 | 原damage仅接受机器人中心严格共线，漏掉连线穿过的格子。改为整数计算格子内部相交，按沿线顺序计算加特林拦截/电磁能量 | 独立有理数裁剪9,600个几何组合、四象限/终点/异阵营拦截、完整Session及200机器人三弹道炮性能 |

这些是实现和既定策略契约修复。没有把“更多墙”“更高金币”“两火箭一电磁”当成已证明的夜战最优策略。

## 验证结果

- 修改前完整套件：224项 PASS，25.401秒。
- 修改后完整套件：**250项 PASS，34.494秒，无跳过**。命令：`.venv\Scripts\python.exe -B -m unittest discover -s tests -q`。
- 新增26项：经济10、战斗9、会话/HTTP6、默认模式长序列1。各类反例先在旧方法上确认失败，再验证修复；既有保护性对照保留。
- 新长序列明确实例化默认 defense：两半场共2,600条观测，分别从0/1起算并镜像地图；每半场独立检查attack、submitAnswer、executeCmd真实出现，覆盖跨日、持续角色/炮损、任务结束、重复缓存、资产清空。夹具占位/合法建造环另经独立枚举检查。
- 全量同时保留原 legacy 2,600条观测、真实HTTP密集输入/四并发重复请求、进程启停、隔离多文件部署、Git Bash执行run.sh、协议及预算回归。本轮使用Windows CPython3.11.10，未重复WSL或冒充正式环境测试。
- `pip check`：No broken requirements found。CRLF感知`git diff --check`通过；提示LF将转换为CRLF属于仓库换行配置，未批量重写文件。
- 只对明确修复的方法调整AST门禁；其余冻结定义、全部返回值/HTTP/非strategic状态对照保留。冻结fixture SHA-256仍为 `105a44200a24a0e5db8c2062e6206f129e3055568b0a6e5f4a10e101b5a76934`。
- legacy的非中心共线射击行为有意修正；固定请求等价通过不表示所有可能输入仍与V2.2完全相同。

## Versus历史地图受控开局对照

使用原有 `tools/replay_day.py`，对Round6全部七个ally日志的初始地图分别运行70个白天回合。基线源码由Git `096c947` 取出到临时目录；修复后同模型、同地图、同defense模式。双方共14次执行全部完成，stderr错误/告警扫描为空。

| 地图 | earned基线→修复后 | 回合70现金基线→修复后 | build总数（双方相同） | 回合70炮等级（双方相同） |
|---|---:|---:|---:|---|
| game18 | 50→50 | 50→50 | 11 | rocket1 / rocket1 / railgun1 |
| game19 | 50→65 | 50→65 | 5 | rocket1 / rocket1 / railgun1 |
| game20 | 100→100 | 0→0 | 6 | rocket2 / rocket1 / railgun1 |
| game21 | 80→80 | 80→80 | 10 | rocket1 / rocket1 / railgun1 |
| game22 | 0→0 | 0→0 | 7 | rocket1 / rocket1 / railgun1 |
| game23 | 50→75 | 50→75 | 5 | rocket1 / rocket1 / railgun1 |
| game24 | 40→40 | 40→40 | 10 | rocket1 / rocket1 / railgun1 |

复现当前单图：`.venv\Scripts\python.exe -B tools/replay_day.py log/Versus/round6/game19/ally_BlueSide_453.log --strategy-mode defense`。

边界：工具根据采样地图构造初态，矿库存/商品等使用其固定假设，没有任务收入、矿刷新或战斗。表中build包括墙和炮，不是墙数；earned只是该模型售矿收益。game19/23多一次售矿，不构成真实比赛收益或生存改善证明。其他地图的资源/墙目标缺口仍存在。

## 独立复核

三条实现线交叉只读复核：战斗审查者复核任务/HTTP及新长序列、冻结门禁；运行时审查者复核弹道/重建及经济改动。复核实际发现并促成修复：VERIFY目标重复资金预留、孤立墙夹具非法坐标/矿位置、长序列动作计数需要按半场分别验收。最新改动已复核，无遗留阻塞项。

主代理再次检查实际源码diff、规格覆盖、测试结果与文档；最终全量在这些源码修复和测试加强之后执行。随后仅更新文档。

## 未解决或无法离线证明的风险

1. 当前E2–E5布局、火力组合、评分权重、增长目标和资金优先级的夜战收益仍未实机验证；Round6七局失败属于旧版本事实，不能从本地PASS推导当前可活1,300回合。
2. 弹道恰好接触格边/角点、同格机器人先后、多发之间预测死亡是否改变后续拦截、`attackPower`多发/溅射语义仍不明确。本次只修明确穿过格子内部的情况。
3. 无任务实例ID/半场请求ID：断档期间未观察到结束的同描述任务无法可靠区分；任意回合回退仍按既有协议契约重置，不能区分迟到旧包和新局。
4. 建设预留仍以BUILD_WEAPON任务存在为条件；无合法炮位、持续动态堵路、缺资源/商品、全背包不可售等情形不保证恢复。只解决本轮可复现的停工/重复预留链，未实现通用最优调度。
5. 默认defense尚未接入NewsPlanner/TreasurePlanner；legacy中的新闻/宝藏测试不是默认模式能力覆盖。真实LLM、沙盒、完整计分与宝藏策略仍缺平台验收。
6. 当前日志不能完整复原逐敌伤害/击杀因果；长期战斗验证仍需要后续实机连续观测。异常日志保护不能为磁盘/系统阻塞提供硬实时五秒保证。

当前适合作为人工Git提交检查点。未自动提交、未启动正式比赛。下一次实机优先核对建造筹资→交付、无效预留释放、任务重新领取、射击合法性与基地/炮手损伤。
