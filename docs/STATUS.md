# AgentRace 当前检查点

## 当前阶段
Versus最新：完成round1五局分析与实现，目标仍为1300回合，未宣称达成。game1按用户纠正红376，红方另233、蓝方246/490/350。新增相对布局、固定运券、危急基地储备、可负担L3优先和有界取石筑墙，独立复核完成。详见VERSUS_ROUND1_ANALYSIS.md。用户自行重命名game1日志，予以保留；本轮改动未提交。下段及127项为历史检查点。
2026-09-10：Self5 分析及修复已落实。用户明确分析包含实现，目标为完整1300回合。新增治疗先于开火、白天备药、冷却时安全邻接站位、炮手附近威胁加权、D-3最后探索、跨日旧矿收益重选。源码及文档未提交，可作为人工提交检查点。实战仍需用户下一次运行验证，不能声称已存活1300回合。

## 当前实现
- 入口 src/main3.py，run.sh 使用该入口。保留用户既有修改、严格协议校验、共享资源预留、事务回滚、缓存和诊断。
- 按具体矿点的采集运输收益评分，保持有效目标，按距离调整出售批量，预留出售和回防时间。
- 默认三火箭，正常优先升级火箭，危急基地优先；采购检查往返、买用和入夜余量。
- 取消人为四命令上限，按剩余回合收束答案；通过比赛沙盒有界发现题目 Markdown 及接口说明，保留材料和 skill。没有硬编码答案。

## 已验证
- Versus：新增8项PASS；完整135项执行中133通过、2项旧建造场景失败；调整场景后只复跑其所在HTTP/开局模块8项PASS（12.219秒）。没有重复全量或WSL，不将原全量命令记为PASS。最终源代码可用测试均已有成功执行覆盖；独立复核无新增阻塞问题。
- 本轮最终单次 Windows 全量：`.venv\Scripts\python.exe -B -m unittest discover -s tests -q`，127项PASS，15.819秒。含真实HTTP、密集输入、并发及两个半场各1300回合观测回放；新增Self5七项。未重复WSL全量。差异空白检查通过。
- 以下120项及Self4经济模型为历史结果，不是本次新策略收益。
- Windows Python 3.11.10：.venv\Scripts\python.exe -B -m unittest discover -s tests -q，120 项 PASS，15.344 秒。
- WSL Python 3.11.10：wsl --exec .venv/linux-test/bin/python -B -m unittest discover -s tests -q，120 项 PASS，22.154 秒。包含真实 HTTP、并发、密集输入及 2600 回合观测回放。
- py_compile、git diff --check 通过；新增七项 Self4 回归。独立审查发现的搜索额度及 JSON 膨胀问题已修复，最终复核无新增阻塞问题。
- 相同初始地图的 70 回合简化经济模型：旧版收入 67、移动 110 次、三座 L1；新版收入 115、移动 94 次、一座 L2 和两座 L1 火箭。模型无刷新、任务和战斗，不是官方模拟器。

## 风险与下一步
- 新版重点验证基地储备/运券、前向布局和后侧炮手、围墙/L3成长。game2/3日志继续不等于存活；game4末尾仍健在。完整1300回合真实战斗尚未验证。
- 已修复的Self5环节需实机核对：Medicine购用、炮手受伤/死亡、冷却移动、D-3探索后提交、跨日矿点重选。Self5六任务一次提前成功、另有48/16金币部分结算；R359全部炮手消失，两座L2仍存。新威胁模型是保守邻近评分，不证明机器人实际目标或1300回合生存。
- 遵循用户测试成本偏好：局部改动只跑相关简洁测试，文档变更不跑套件；最终交付或广泛影响时单环境完整验证，平台相关才追加跨环境。
- Self4 五次答案提交失败，金币停在 62，没有采购升级。日志缺完整任务、实际命令和答案，不能确定每道题失败原因。
- 真实解题成功率、三火箭效果及动态地图下采购完成率需用户下一次平台运行验证。不同场次 demo 不能用于比较胜率。
- 动态 attackPower、多发/同回合死亡及擦边弹道仍有语义缺口；中途接入需显式 round-origin，重启不恢复任务。
- 用户负责正式平台验证。下一份日志重点核对文件发现结果、提交反馈、首夜武器等级、售矿/买用和回防时间。详见 TEST_PLAN.md 和 REVIEW.md。
