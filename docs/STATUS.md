# AgentRace 当前检查点

## 当前阶段

P3建设/升级/夜战已有实现并接入默认规划器；恢复复查与本地测试完成一轮，后续继续P3覆盖审计及P4设计。整体 NOT READY。

## 已完成与实现现状

- P0安全三字段响应、结构校验、错误回退、UTF-8、端口检查和run.sh。
- Phase、World、基地/建造环、八邻域BFS、移动预留；历史敌情不参与当前障碍。
- GameSession串行事务、副本规划后提交、最近同回合请求去重、冲突拒绝、失败回滚、新闻事件与敌情历史、反馈连续回合归属、身份/回合回退重置；callback已集成；默认规划器已运行基础经济策略。
- 可用 `--round-origin 0|1` 指定确认的起点；未配置只从实际回合0识别，否则phase未知。
- P2已有12动作状态校验、共享金币/控制者/目标预留、背包多重集、商店实时价格、采矿/卖矿/采购接口和召唤令日额度。恢复后修复非目标消耗品多余坐标及大整数生命值转换溢出。
- P3 DefensePlanner已接入建造、升级、修墙、控制者站位、三类武器选点和消耗品；恢复后补齐低等级墙使用WallFixer，满血不消耗。
- 未访问Official/；未新增依赖；未自动提交。保留全部未提交修改。

## 已验证

- 本次全套54项PASS（0.890秒），包含受审查权限真实HTTP启停；普通沙箱53项运行曾因socket权限产生1项ERROR，随后完整重跑成功。
- 动作/经济19、状态9、世界6、协议5、HTTP2、防守13；防守含200机器人完整callback时限与重复请求、各等级围墙修复回归。
- py_compile和git diff --check成功。P3局部规格复查已执行；全规格覆盖与最终独立审查尚未完成。

## 风险/未决

- 已有基础经济与建设夜战；任务/宝藏策略尚未完成，不能参赛。
- 协议缺请求/半场ID及乱序保证；回合回退重置按§65建议实现，无法区分同身份延迟旧请求与新半场。同回合不同内容保守拒绝；本地去重不证明平台恰好一次执行。
- Robot外层未展开，当前支持数组/roles包装，正式兼容未验证。
- 围墙成本、build.name、roundNo起点、基地占格、弹道边界、任务重试/沙盒持久性及半场生命周期待平台确认，详见DESIGN。
- 正式Python 3.11.10未验证；既有环境记录为.venv Python3.10.12 + Flask3.1.3，本阶段未重测解释器版本。

## 下一步

1. 继续P3覆盖审计（采购/维修调度、密集场景HTTP时限及CLI配置）；随后阅读§31–38、51开始P4异步任务状态机设计。未确认的建造名称/墙成本保持显式配置。
2. P4任务技能、P5新闻宝藏、P6长局集成、全规格覆盖与最终独立审查。
3. 保留平台未决项；每阶段更新文档和README，不自动提交。
