# AgentRace 当前检查点

## 当前阶段

2026-09-10：已完成Self/3.log逐回合分析：97回合仅2回合整回合空动作，但0卖矿、0任务提交，三次任务全部超时；发现减员后固定开火顺序阻碍火箭炮手补位。此次仅分析和文档更新，策略尚未修复，不宣称比赛就绪。

## 当前实现与证据

- 入口为 `src/main3.py <port>`，监听0.0.0.0；保留严格三字段JSON、动作校验、共享资源预留、事务回滚和重复请求缓存。前3次HTTP交换输出状态诊断。
- 已实现世界解析/寻路、采集销售、建造维护、武器操控、任务LLM/沙盒流程和新闻宝藏策略。完整架构见DESIGN.md，需求覆盖见COVERAGE.md。
- Self日志15条摘要全部phase=null；实际开局roundNo=1，第69回合325金币仍无武器，第87回合减员且基地受损。移动反馈及坐标变化确认角色确有移动。详见SELF_LOG_ANALYSIS.md。
- 首观测0/1自动识别回合起点，显式配置保留；中途接入需--round-origin。开火/回防先于新任务和宝藏，活动任务先锋仍受平台限制。
- [trace_turn]记录背包矿物、任务状态、武器/机器人数：前10次、每10次及昼夜边界采样，异常额外限量；[trace_map]在首观测、每50次及边界输出当前41×32全图。诊断异常不替换已提交响应。

## 最新运行与验证

- Self/3.log：97条连续回合，191动作、190条反馈均true（只证明合法性）；R77/78空动作，火箭冷却3/2。R4–97金币0；19条沙盒命令、0答案提交，R24/43/68任务超时。R91先锋消失，R92–97火箭冷却0但无人补位。完整证据及推断边界见SELF_LOG_ANALYSIS.md。此次仅日志统计与代码/规格审查，未重跑套件。

- Self/2.log：105条HTTP、22条摘要，已记录摘要均有动作；R4三座武器存在，R80反馈确认火箭曾攻击成功。R100基地665血，防御效果仍差。详见SELF_LOG_ANALYSIS.md最新章节。
- 新增每回合[turn]短摘要，区分武器操控/任务占用/未下令，记录武器等级、冷却、射程及邻接角色；不因no_command擅自断言无目标。重复请求不重复输出。
- 最新完整命令`.venv/bin/python -B -m unittest discover -s tests -q`：101项PASS（9.729秒，允许本地网络），含每回合诊断/冷却驻守/缓存专项及真实HTTP、并发、密集输入、集成回放。

- 中断前完整100项PASS（9.032秒），包括真实HTTP、并发重复请求、密集观测、2600回合观测回放和4项新增实测回归。
- 本次续接首次完整运行：100项中2项HTTP测试因沙箱禁止socket失败，其余98项通过；随后允许本地网络重跑完整100项PASS（9.266秒），无跳过。
- 开局回归只应用移动/建造结果，验证首回合建造、三座武器及第71回合攻击输出，不是官方战斗模拟。
- 已单独复核昼夜边界、防御调度、活动任务隔离、地图占地、锁内缓存及诊断故障隔离；git diff空白检查通过。此前独立审查见REVIEW.md，不代表新增修改经过子Agent审查。

## 未验证与风险

- 真实建造/伤害结算、防御布局效果、经济往返收益和胜率尚未复测。
- 任务错误码1为任务超时；LLM返回、格式、探索次数或题目难度何者导致超时，旧日志不足以定位。
- 旧日志没有完整地图与背包，不能重建当时全局局势。新地图是当前观测，单位覆盖同格地物。
- attackPower多发/溅射、擦边弹道、同回合死亡拦截语义缺平台证据。正式LLM/沙盒、CentOS未联调。
- 无请求/半场ID；同身份回合回退存在旧请求与新半场歧义。进程重启不恢复任务或配额。

## 下一步与交付

本地后续策略工作：领取任务前预留时限与回防路程；经济评分纳入商人距离及提前变现；采购升级预留日间路程，分别补动态路径回归。用户下次运行可保留新增[turn]完整过程，进一步验证炮手/冷却/任务异步状态；无需重复确认建造是否启用。

当前修改未提交，保留已有工作；本地验证已完成，可作为人工Git提交检查点。历史测试与交付细节见TEST_PLAN.md、RUNBOOK.md及REVIEW.md。
