# AgentRace 当前检查点

## 当前阶段

按用户划定范围，本地开发交付完成：需求分析、设计、P0–P5实现、P6测试、52项需求覆盖审计和独立只读代码审查均已完成。正式判题、平台联调和人工接管由用户负责；不宣称正式比赛就绪。

## 已实现

- 严格三字段JSON、12动作门禁、共享金币/角色/建筑预留、异常回退、事务回滚、重复请求隔离。
- 当前观测世界、基地/建造环、八邻域寻路、经济、建造/维护、三种武器开火、消耗品与召唤令。
- 任务LLM→沙盒→答案异步流程、权限/配额/结果归属、故障反馈和未验证经验；新闻证据提取、跨日原文与宝藏采购/献祭。
- 武器build.name按用户确认默认采用gatling/railgun/rocket，CLI可替换映射集合；围墙默认消耗1石头，依据官方内嵌完整图。
- Windows .venv和WSL .venv/linux-test均为CPython3.11.10，锁定依赖齐全；run.sh使用LF。

## 验证与审查

- Windows：`.venv\Scripts\python.exe -B -m unittest discover -s tests -q`，92项PASS，10.308秒。
- WSL：`wsl --exec .venv/linux-test/bin/python -B -m unittest discover -s tests -q`，92项PASS，17.223秒，包含实际run.sh。
- 动作23、防守18、HTTP4、集成2、状态9、新闻9、协议6、任务15、世界6；实际文件测试数量已核对。
- 1302机器人密集HTTP四并发重复请求及后续请求均小于5秒；2600回合是观测回放，不是战斗模拟或胜率验证。
- 最小运行文件复制启动在Windows/WSL均通过；py_compile、两环境pip check、git diff --check通过。
- 独立只读子Agent完成规格/代码/测试审查，独立核心67项PASS，未发现新增确定性协议错误或必现崩溃。已记录具体物理假设风险。详见REVIEW.md。

## 用户接手的验证与残余风险

- roundNo起点仍需确认；支持--round-origin 0或1，未配置仅从实际0识别。不要把服务已启动等同于昼夜已识别。
- attackPower多发/溅射含义、非共线擦边弹道、同回合预测死亡后是否继续拦截尚无平台证据；伤害估值不等于实际结算。
- 正式LLM、沙盒、真实谜题、CentOS未联调；完整原文保留不保证外部LLM容量。任务动作合法/结束不等于全对，经验均标未验证。
- 无请求/半场ID；同身份回合回退无法区分延迟旧请求与新半场，端到端恰好一次执行无法证明。进程重启不恢复当前任务/配额。

## 交付入口

RUNBOOK.md给出准确Windows/WSL启动命令与人工接管检查；DESIGN.md记录方案；TEST_PLAN.md记录命令与证据；COVERAGE.md逐条映射需求；REVIEW.md记录独立审查结论和风险。

代码和文档修改尚未提交，可作为人工Git提交检查点。正式判题发现问题后，提供对应观测/响应/反馈即可继续定位；不需要本机访问比赛网络。

## 本次入口调整（2026-09-10）

用户已将 src/main.py 改为 src/main3.py；本次保留该改名，统一脚本、测试和当前文档入口。按用户指定官方 SDK 改为 Flask 默认监听地址 127.0.0.1，取代原 0.0.0.0 工程建议；端口仍为首个位置参数，可选配置保留。正式判题网络可达性仍需用户验证。当前验证结果见 TEST_PLAN.md 本次入口验证。

本次 Linux CPython 3.10.12 全套 92 项 PASS（10.751 秒），含直接入口与脚本真实 HTTP；已复核入口差异和相关调用，CRLF 感知 diff 检查通过。可作为人工提交检查点，未创建提交。
