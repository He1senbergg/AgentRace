# 当前状态：V3.6 候选版

以根目录README和V3.6审计为准。当前生产源src/main3.py；单文件入口CoreGeek/main3.py；提交CoreGeek.tar.gz。

不是已经证明能存活1300回合的版本。105回归、81任务命令、22实际HTTP、8936历史帧仅用于相应离线范围验证。

本版保留14墙，前线受限维护和武器链预算保护；不要恢复C12实验、旧一整圈墙、或每个白天/黑夜固定停工策略。不改已有效的任务执行器，不加入抢对手积分路线。

## 日志下载工具（2026-09-15）

`coregeek_logs.py` 默认输出已改为当前工作目录的 `log/`，保留 round/game 递增与指定轮次补下载；用法见 README 第 6 节。目录与编号边界检查通过（首次、预览不落盘、跨轮续号、补下载、轮次不存在）。实际使用 Windows Python + Edge 复用专用登录目录，练习赛第 1 页下载 8 场、16 个非空日志至 `log/round14/game91`–`game98`，0 错误，2 场网站禁用下载；再次 `--round 14` 校验并跳过全部 16 个文件，0 错误。summary.json 保存最近一次补下载汇总。此验证仅覆盖日志工具，不改变 V3.6 比赛验收结论。