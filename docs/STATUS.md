# AgentRace 当前检查点

## 当前阶段
2026-09-10：完成 Self/4.log 和官方 demo 日志分析、经济/防御/任务修复、双环境完整测试及独立只读审查。修改未提交，可作为人工提交检查点；不宣称实战胜率已验证。详细证据见 SELF4_ANALYSIS.md。

## 当前实现
- 入口 src/main3.py，run.sh 使用该入口。保留用户既有修改、严格协议校验、共享资源预留、事务回滚、缓存和诊断。
- 按具体矿点的采集运输收益评分，保持有效目标，按距离调整出售批量，预留出售和回防时间。
- 默认三火箭，正常优先升级火箭，危急基地优先；采购检查往返、买用和入夜余量。
- 取消人为四命令上限，按剩余回合收束答案；通过比赛沙盒有界发现题目 Markdown 及接口说明，保留材料和 skill。没有硬编码答案。

## 已验证
- Windows Python 3.11.10：.venv\Scripts\python.exe -B -m unittest discover -s tests -q，120 项 PASS，15.344 秒。
- WSL Python 3.11.10：wsl --exec .venv/linux-test/bin/python -B -m unittest discover -s tests -q，120 项 PASS，22.154 秒。包含真实 HTTP、并发、密集输入及 2600 回合观测回放。
- py_compile、git diff --check 通过；新增七项 Self4 回归。独立审查发现的搜索额度及 JSON 膨胀问题已修复，最终复核无新增阻塞问题。
- 相同初始地图的 70 回合简化经济模型：旧版收入 67、移动 110 次、三座 L1；新版收入 115、移动 94 次、一座 L2 和两座 L1 火箭。模型无刷新、任务和战斗，不是官方模拟器。

## 风险与下一步
- Self4 五次答案提交失败，金币停在 62，没有采购升级。日志缺完整任务、实际命令和答案，不能确定每道题失败原因。
- 真实解题成功率、三火箭效果及动态地图下采购完成率需用户下一次平台运行验证。不同场次 demo 不能用于比较胜率。
- 动态 attackPower、多发/同回合死亡及擦边弹道仍有语义缺口；中途接入需显式 round-origin，重启不恢复任务。
- 用户负责正式平台验证。下一份日志重点核对文件发现结果、提交反馈、首夜武器等级、售矿/买用和回防时间。详见 TEST_PLAN.md 和 REVIEW.md。
